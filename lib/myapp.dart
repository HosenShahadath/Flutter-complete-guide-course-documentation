import 'package:flutter/material.dart';
import 'section1/basic flutter program/second_app.dart';

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: SecondApp(),
    );
  }
}
